{
  "name": "mlx",
  "version": "0.0.1",
  "description": "MLX for Node 22 with Metal GPU (experimental)",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npm run build:native && npm run report",
    "build:native": "npx node-gyp configure build",
    "report": "node ../tools/src/ASTDistance/typescript/dist/cli.js deep ../python/mlx python ./src typescript > REPORT.md",
    "report:cpp": "node ../tools/src/ASTDistance/typescript/dist/cli.js deep ../mlx cpp ./src/native cpp > REPORT_CPP.md",
    "test": "npx mocha --require tsx --extension ts \"test/core/*.test.ts\" \"test/*.test.ts\"",
    "ci": "npm run build:native && npm test",
    "prepack": "npm run build:native",
    "pack:dry-run": "npm pack --dry-run"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "mlx",
    "metal",
    "gpu",
    "ml",
    "ai",
    "apple",
    "node-addon-api"
  ],
  "files": [
    "index.js",
    "build/Release/mlx.node"
  ],
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "mocha": "^11.7.5",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0"
  },
  "dependencies": {
    "nan": "^2.25.0",
    "node-addon-api": "^8.5.0"
  }
}
